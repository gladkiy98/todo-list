for(i = 0; i < $('#container').find('.active').length; i++) {
  task.changeNumber('-');
}
$('#container').html("<%= j (render partial: 'task', locals: { tasks: @tasks } ) %>");
task.checkedAll('<%= active_all_tasks_path %>');
