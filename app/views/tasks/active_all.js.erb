for(i = 0; i < $('.row-task').length; i++) {
  task.changeNumber('+');
}
$('#container').html("<%= j (render partial: 'task', locals: { tasks: @tasks } ) %>");
task.checkedAll('<%= completed_all_tasks_path %>');
